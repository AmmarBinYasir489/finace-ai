"""Financial OS — agent pipeline scaffolding.

This package layers the spec's strictly-isolated agent architecture on top of the
existing VoiceTrack code. Modules here are additive: they do not replace the
working extractor/db pipeline until each stage is wired in deliberately.
"""
